## Dropbox App Server Side

1. At the root of the project run `$ npm install`.
2. cd nodelogin.
3. The back-end server is up and running.

## Dropbox App Client Side
1. At the root of the project run `$ npm install`.
2. cd reactlogin.
3. The front-end server is up and running.
